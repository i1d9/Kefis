defmodule KefisWeb.Retailer.ProductDetailsLive do
  use KefisWeb, :live_component

  def update(assig, socket) do
    {:ok, socket}
  end

  def render(assigns) do
    ~H"""
    <div>
    </div>
    """
  end
end
