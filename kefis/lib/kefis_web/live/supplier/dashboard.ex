defmodule KefisWeb.Supplier.Dashboard do
  use KefisWeb, :live_component


  def update(assigns, socket) do
    {:ok, socket}
  end


  def render(assigns) do
    ~H"""
    <div></div>
    """
  end
end
